= search_form_for @qw, url: queen_works_path, class: 'navbar-form' do |f|
  .input-group
    .input-group-btn
      %button.btn.btn-default.dropdown-toggle{"data-toggle"=>'dropdown',:type=>'button'}
        搜案例
        %span.caret
      %ul.dropdown-menu{:role=>"menu"}
        %li
          = link_to '找蚁后', search_dropdown_index_path(search: :queens), remote: true
          = link_to '搜案例', search_dropdown_index_path(search: :queen_works), remote: true
    = text_field_tag('q[title_cont]', nil, class: 'form-control')
    .input-group-btn
      = f.button '', class: 'btn btn-default' do
        %span.glyphicon.glyphicon-search
  .nav_quick_btn
    %a{:href=>new_need_path} 发项目
    %a{:href=>queen_works_path} 搜案例
    %a{:href=>queens_path} 找蚁后
