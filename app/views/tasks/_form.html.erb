<%= simple_form_for(task) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :dead_line %>
    <%= f.input :title %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
